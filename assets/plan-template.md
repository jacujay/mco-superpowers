# Plan: {Feature Name}

> Generated by superpowers/writing-plans mode

## Goal

{One sentence describing what this plan achieves}

## Architecture

{2-3 sentences explaining the high-level approach}

## Tech Stack

{List key technologies, frameworks, and tools}

## Files

### New Files
| File | Purpose |
|------|---------|
| `path/to/file.ts` | {description} |

### Modified Files
| File | Changes |
|------|---------|
| `path/to/existing.ts` | {what changes and why} |

## Tasks

> Each task should take 2-5 minutes. Follow TDD: write test → watch fail → implement → watch pass → commit.

### Task 1: {Title}

**Files**: `path/to/file.ts`
**Test**: `path/to/file.test.ts`

Steps:
1. Write failing test:
   ```typescript
   // test code example
   ```
2. Implement minimal code to pass
3. Verify: `npm test -- --grep "test name"`
4. Commit: `git commit -m "feat: {description}"`

### Task 2: {Title}

{Same structure as Task 1}

## Execution

Choose one:
- [ ] **Inline execution** — follow executing-plans mode, do tasks sequentially
- [ ] **Subagent-driven** — follow subagent-driven-development mode, dispatch per task
- [ ] **Multi-agent parallel** — use dispatching-parallel-agents for independent tasks (OpenClaw only)

## Verification

After all tasks complete:
1. Run full test suite: `{test command}`
2. Expected: all tests pass, no regressions
3. Run linter: `{lint command}`
4. Manual check: {what to verify manually}
